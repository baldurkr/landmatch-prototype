
  # LandMatch Prototype

  This is a code bundle for LandMatch Prototype. The original project is available at https://www.figma.com/design/SGs6QYSs7lOvtqVGm8mfAa/LandMatch-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  