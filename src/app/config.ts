// Global app configuration / feature flags.

// When true, the map renders static images from src/assets/maps instead of the
// live, interactive OpenStreetMap (Leaflet) map. Used for testing/prototyping.
// Flip to false to restore the fully functional OSM map.
export const USE_STATIC_MAP = true;
